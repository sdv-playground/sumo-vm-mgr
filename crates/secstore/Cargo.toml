[package]
name = "secstore"
version = "0.1.0"
edition = "2021"
description = "Encrypted key metadata persistence with pluggable backends"

[dependencies]
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
